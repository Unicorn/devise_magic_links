require 'spec_helper'

describe DeviseMagicLinks do
  it 'has a version number' do
    expect(DeviseMagicLinks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
